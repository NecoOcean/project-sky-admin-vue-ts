version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sky-admin-vue
    ports:
      - "8080:80"
    restart: unless-stopped
    # 如果需要连接后端服务，可以添加网络配置
    # networks:
    #   - app-network

# networks:
#   app-network:
#     driver: bridge

